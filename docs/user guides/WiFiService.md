# WiFiService
Warning : this document is generated by GHCopilot.
## Overview

The **WiFiService** is a critical infrastructure service that manages all WiFi connectivity for the K10 Bot. It handles both WiFi station mode (connecting to an access point) and access point mode (creating its own network) with automatic fallback functionality.

## Service Information

- **Service Name**: `WiFi Service`
- **Class**: `WifiService`
- **Header**: [src/services/wifi/WiFiService.h](../src/services/implementations/wifi/WiFiService.h)
- **Implementation**: [src/services/wifi/WiFiService.cpp](../src/services/implementations/wifi/WiFiService.cpp)
- **Inherits**: `IsServiceInterface`

## Features

### Core Functionality
- **Station Mode**: Connect to an existing WiFi access point
- **Access Point Mode**: Create a WiFi access point when connection fails
- **Automatic Fallback**: Automatically switches to AP mode if station connection fails
- **Connection Management**: Handles WiFi connect/disconnect operations
- **Network Information**: Provides IP address, SSID, and hostname information

### Configuration Persistence
- Settings are saved via `SettingsService` integration
- Automatic loading of previous WiFi credentials on startup
- Persistent hostname and network preferences

## API Methods

### Public Methods

#### `bool wifi_activation()`
Activates WiFi with automatic fallback: tries STA, opens AP on failure. Returns `true` if either mode is active.

#### `std::string getIP()`
Returns the current IP address (STA local IP or AP gateway IP).

#### `std::string getSSID()`
Returns the currently active SSID (connected network or own AP name).

#### `std::string getHostname()`
Returns the device hostname: base name + 6-char uppercase MAC suffix (e.g. `amakerbot-A3F2B1`).

#### `std::string getWifiSsid() const`
Returns the configured STA SSID held in memory (may differ from what the device is currently connected to if changed but not applied).

#### `std::string getWifiPassword() const`
Returns the configured STA password held in memory.

#### `void setWifiCredentials(const std::string &ssid, const std::string &password)`
Updates the in-memory STA credentials. Does **not** persist to NVS — call `saveSettings()` afterwards, or `applySettings()` to also reconnect.

#### `bool applySettings()`
Disconnects and reconnects using the current in-memory credentials (STA with AP fallback). Call after `setWifiCredentials()` + `saveSettings()` to activate changes without rebooting.

#### Service Lifecycle Methods
- `bool initializeService()` — loads NVS credentials via `loadSettings()`, sets status `INITIALIZED`
- `bool startService()` — calls `connect_and_fallback()` with the current credentials
- `bool stopService()` — disconnects and clears IP/SSID state
- `bool saveSettings()` — writes all credentials to NVS (`Preferences` namespace `"wifi"`)
- `bool loadSettings()` — reads credentials from NVS; silently keeps compile-time defaults if namespace absent
- `bool resetSettings()` — clears the NVS `"wifi"` namespace and restores compile-time defaults in memory (does **not** reconnect)

### Protected Methods

#### `bool open_access_point()`
Creates a WiFi SoftAP. Full SSID = `ap_ssid_` + MAC suffix (default: `aMaker-XXXXXX`).

#### `bool connect_to_wifi(ssid, password)`
Tries to connect up to `wifi_conn_max_attempts` (8) times, sleeping `wifi_conn_sleep_ms` (500 ms) between attempts.

#### `bool disconnect_from_wifi()`
Calls `WiFi.disconnect(true)` and clears `connected_ip_` / `connected_ssid_`.

#### `bool connect_and_fallback(ssid, password)`
Tries STA; opens AP on failure.

## NVS Storage

All credentials are persisted under the `Preferences` namespace `"wifi"`:

| NVS key | Default value | Description |
|---|---|---|
| `ssid` | `Freebox-A35871` *(dev only)* | STA SSID |
| `password` | *(dev default)* | STA password |
| `ap_ssid` | `aMaker-` | AP SSID base |
| `ap_password` | `amaker-club` | AP password |
| `hostname` | `amakerbot-` | Hostname base |

## Usage Example

### In main.cpp

```cpp
#include "services/WiFiService.h"

WifiService wifi_service = WifiService();

void setup() {
    // Initialize and start WiFi service
    if (!start_service(wifi_service)) {
        logger.error("Failed to start WiFi service");
        return;
    }
    
    // Get network information
    String ip = wifi_service.getIP().c_str();
    String ssid = wifi_service.getSSID().c_str();
    
    logger.info("Connected to: " + ssid);
    logger.info("IP Address: " + ip);
}
```

## Integration Points

### With Other Services
- **BotServerWebSocket / BotServerUDP**: Require WiFi to be active. Started after WiFiService in `setup()`.
- **AmakerBotService**: WiFi settings are exposed remotely via commands `0x47` / `0x48` / `0x49`. Inject with `amaker_bot_.setWifiService(&wifi_service_)`.
- **AmakerBotUIService**: Displays the connected IP and SSID on the TFT screen.

### Startup Sequence
The WiFiService must be started **before** any network-dependent services:
```cpp
// In main.cpp setup()
start_service(wifi_service);        // First - establish network
start_service(http_service);        // Then - start web server
start_service(udp_service);         // Then - start UDP service
```

## Connection Flow

```
┌─────────────────┐
│  Initialize     │
│  WiFi Hardware  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Load Saved     │
│  Credentials    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     Success    ┌──────────────┐
│  Try Connect    ├───────────────►│ Station Mode │
│  to WiFi AP     │                │   Connected  │
└────────┬────────┘                └──────────────┘
         │
         │ Failure
         ▼
┌─────────────────┐                ┌──────────────┐
│  Create Access  ├───────────────►│   AP Mode    │
│     Point       │                │    Active    │
└─────────────────┘                └──────────────┘
```

## Error Handling

The WiFiService implements robust error handling:
- Connection timeouts trigger automatic AP mode
- Failed AP creation is logged as critical error
- WiFi disconnections are detected and can trigger reconnection
- All errors are logged via `RollingLogger`

## Best Practices

1. **Always check WiFi status** before starting network services
2. **Handle both modes** in dependent services (station vs AP)
3. **Don't block on WiFi** operations - use timeouts
4. **Log connection events** for debugging
5. **Test AP fallback** to ensure robot remains accessible

## Troubleshooting

### WiFi Won't Connect
- Verify SSID and password in settings
- Check WiFi signal strength
- Ensure 2.4GHz network (ESP32-S3 doesn't support 5GHz)
- Check for MAC filtering on router

### AP Mode Issues
- Verify no other device is using `192.168.4.1`
- Check AP password meets ESP32 requirements (min 8 characters)
- Ensure power supply is adequate

### IP Address Not Displayed
- WiFi service may not be fully started
- Check serial monitor for connection messages
- Verify UI is refreshing after connection

## Related Documentation

- [IsServiceInterface.md](IsServiceInterface.md) - Base service interface
- [HTTPService Documentation](readme.md#httpservice) - Web server integration
- [UDPService Documentation](UDPServiceHandlers.md) - UDP service integration
